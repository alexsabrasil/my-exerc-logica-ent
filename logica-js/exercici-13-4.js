function subtract(a, b) {
    return a - b;
  }
  
  const numero1 = 10;
  const numero2 = 3;
  
  const resultado = subtract(numero1, numero2);
  console.log(resultado); // Isso imprimirá 7 no console
  